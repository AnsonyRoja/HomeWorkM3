const {suma} = require('./funciones');

// common js es nativo de node, es decir no necesita configuracion ni nada
// sino que ya viene predeterminado para que funcione de esta manera. 

console.log(suma(2,3))